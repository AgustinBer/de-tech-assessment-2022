CREATE TABLE events (
    event VARCHAR(255),
    entity VARCHAR(255),
    timestamp TIMESTAMP,
    organization VARCHAR(255),
    vehicle VARCHAR(255),
    lat NUMERIC,
    lng NUMERIC,
    start TIMESTAMP,
    finish TIMESTAMP
);
